import React from 'react';
import TransitionGroup from 'react-transition-group/Transition';
import Spash from './splash.jsx';

const SplashContainer = ({ in: inProp }) => {
	<TransitionGroup component='div'>
		
	</TransitionGroup>
}
